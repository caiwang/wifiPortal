#script to create video database in mysql

create database video;

use video;
drop table video;
create table video(
  id BIGINT(20) not null auto_increment, 
  name varchar(128) not null, 
  url varchar(250) not null, 
  duration BIGINT(20),
  primary key(id)
);

### for more info., go to link:
https://github.com/caiwang/iserver/blob/master/db.schema/wlsp.sys.base.sql

https://github.com/caiwang/iserver is the place for wlsp database creation.